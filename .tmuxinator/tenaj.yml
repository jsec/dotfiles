# ~/.tmuxinator/eflex.yml

name: tenaj
root: ~/src/web/mom/

# Optional tmux socket
# socket_name: foo

# Runs before everything. Use it to start daemons etc.
# pre:

windows:
  - editor:
      layout: eed3,204x55,0,0[204x41,0,0,1,204x13,0,42{102x13,0,42,2,101x13,103,42,0}]
      panes:
        - vim
        - brunch w -s
        -
