" Restore title (I already know I'm using Pentadactyl, but thanks!)
set titlestring=Mozilla\ Firefox

" Remap hint keys to use home row
set hintkeys=asdfjkl;

" Color stuff
colorscheme darkblue

" ~~~MAPPINGS~~~
" faster scrolling
nmap -b j 5j
nmap -b k 5k

" faster exit to cmd mode
imap jj <ESC>

" vim: set filetype=vim :
